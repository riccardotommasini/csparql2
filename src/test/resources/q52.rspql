PREFIX ars: <http://www.streamreasoning/it.polimi.jasper.test/artist#>

REGISTER RSTREAM <s1> AS
SELECT ?a ?age
FROM NAMED WINDOW <win2> ON <stream2> [RANGE PT10S STEP PT10S]
WHERE  {
    WINDOW ?w {
        ?a a ars:Writer ;
           ars:hasAge ?age .
    }
}